export component Infoholder inherits Rectangle {
	background: rgba(0, 0, 0, 0.3);
	border-radius: 18px;
	border-width: 1px;
	border-color: #434343;
	height: 54px;

	VerticalLayout {
		alignment: center;
		HorizontalLayout {
			padding: 12px;
			alignment: center;
			spacing: 16px;

			@children
		}
	}
}
